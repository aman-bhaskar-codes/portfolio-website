# ═══════════════════════════════════════════════════════════
# Agentic Portfolio Backend — Python Dependencies
# ═══════════════════════════════════════════════════════════

# ── Core Framework ──
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
pydantic==2.10.4
pydantic-settings==2.7.1

# ── Database ──
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.1
pgvector==0.3.6

# ── Redis ──
redis[hiredis]==5.2.1

# ── Vector Store ──
qdrant-client==1.12.1

# ── LLM & Agents ──
langchain==0.3.14
langchain-community==0.3.14
langgraph==0.2.60
tiktoken==0.8.0

# ── Embeddings & Reranking ──
sentence-transformers==3.3.1
rank-bm25==0.2.2

# ── Document Parsing ──
docling==2.14.0

# ── Celery (Task Queue) ──
celery[redis]==5.4.0

# ── Observability ──
langfuse==2.57.2
prometheus-client==0.21.1
structlog==24.4.0
sentry-sdk[fastapi]==2.19.2

# ── HTTP Client ──
httpx==0.28.1

# ── Security ──
python-jose[cryptography]==3.3.0
bleach==6.2.0

# ── Social API Tools ──
PyGithub==2.5.0

# ── Utilities ──
python-dotenv==1.0.1
python-multipart==0.0.20
