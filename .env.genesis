# ═══════════════════════════════════════════════════════════
# ANTIGRAVITY OS — Environment Template
# ═══════════════════════════════════════════════════════════
# INSTRUCTIONS:
#   1. Copy this file: cp .env.genesis .env
#   2. Generate secrets: openssl rand -hex 32
#   3. Fill in API tokens
#   4. NEVER commit .env to git
# ═══════════════════════════════════════════════════════════

# ── OWNER IDENTITY ─────────────────────────────────────────
OWNER_NAME=Aman Bhaskar
OWNER_TITLE=Agentic AI Developer
OWNER_BIO=Building AI systems that are resilient, local-first, and production-grade.

# ── DATABASE (PostgreSQL + pgvector) ───────────────────────
POSTGRES_PASSWORD=            # REQUIRED — generate: openssl rand -base64 24
POSTGRES_USER=postgres
POSTGRES_DB=portfolio_db

# ── QDRANT ─────────────────────────────────────────────────
QDRANT_COLLECTION_KNOWLEDGE=portfolio_knowledge
QDRANT_COLLECTION_MEMORIES=user_memories

# ── OLLAMA (local AI — runs on your machine) ──────────────
OLLAMA_URL=http://host.docker.internal:11434
OLLAMA_TIMEOUT=120
EMBED_MODEL=nomic-embed-text

# ── LLM MODEL ROUTING ─────────────────────────────────────
LLM_MODEL_HEAVY=phi4-mini
LLM_MODEL_MEDIUM=llama3.2:3b
LLM_MODEL_LIGHT=qwen2.5:3b

# ── SECRETS (generate each: openssl rand -hex 32) ─────────
JWT_SECRET=                   # REQUIRED
VAULT_ENCRYPTION_KEY=         # REQUIRED
NEXTAUTH_SECRET=              # REQUIRED

# ── GITHUB ─────────────────────────────────────────────────
GITHUB_TOKEN=                 # Fine-grained PAT with read-only repo access
GITHUB_USERNAME=aman-bhaskar-codes

# ── LANGFUSE (LLM Observability) ──────────────────────────
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_NEXTAUTH_SECRET=     # REQUIRED — generate: openssl rand -hex 32
LANGFUSE_SALT=                # REQUIRED — generate: openssl rand -hex 16

# ── RATE LIMITS ────────────────────────────────────────────
RATE_LIMIT_ANON_PER_MIN=20
RATE_LIMIT_AUTH_PER_MIN=60

# ── FEATURE FLAGS ──────────────────────────────────────────
FEATURE_VOICE_MODE=true
FEATURE_3D_CONSTELLATION=true
FEATURE_CLI_MODE=true
FEATURE_BUILD_WITH_ME=true

# ── RUNTIME ────────────────────────────────────────────────
ENVIRONMENT=development
DEBUG=false
LOG_LEVEL=INFO
